// The fixed set of categories every expense must belong to.
// Centralised here so the form, filter, and summary all stay in sync.
export const CATEGORIES = [
  'Food',
  'Transport',
  'Housing',
  'Entertainment',
  'Health',
  'Other',
]

// Currency symbol used throughout the UI. Change in one place to re-brand.
export const CURRENCY = '$'
